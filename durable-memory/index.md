# lmcode — Portal Memory (Index)

Shared, provider-agnostic, multi-file memory for the lmcode project (an OSS opencode
fork being reshaped into a plain CLI for LLM/agent use). Read this first on every fresh
session. This directory is the "portal": it is committed to the repo so the memory is
externally persisted and survives any single agent's session.

## What lmcode is

A Bun + TypeScript monorepo (Turborepo) for an AI coding agent: server, CLI, TUI, SDK,
web/desktop frontends. Runtime built heavily on Effect v4 / effect-smol. Default branch: `dev`.
Direction: lmcode is for AGENTS, not humans — a plain CLI where everything GUI/TUI does is
reachable via commands + editable config (with a verify step). The TUI is kept but no longer
the default.

## Docs in this portal (each a focused, LLM-friendly file)

- `notification-output-limit.md` — NOTIFICATION-OUTPUT-LIMIT: mandatory Codex OAuth cap omission for unattended turns without re-enabling plugin hooks.
- `filesystem-default-access.md` — FS-DEFAULT-ACCESS: default cross-directory access in both runtimes, explicit restriction precedence, and structured Linux workdir handling.
- `provider-openai-astra.md` — verified `openai/gpt-6-astra` via Codex OAuth, exact-name filter fix, older-binary declaration workaround, published example, and clean diagram lint.
- `direction.md` — operator north star: CLI + single-user sequential REST microservice, file-based permissions (no popup), standalone config (no parent/sibling merge).
- `runbook.md` — how to run/auth/test lmcode in dev (toolchain, copilot OAuth device flow, default model, wrapper).
- `cli-commands.md` — the agent-facing CLI surface added in this work (models, auth list, run --effort, config, default command, session commands, stats with provider/model filters and JSON).
- `models-and-effort.md` — model selection, variants/effort tiers (xhigh = "extra high"), entitled list + which models actually work.
- `config-cli.md` — config schema, writers (updateGlobal/patchJsonc), precedence/shadowing, effort-default decision, non-interactive gaps.
- `build.md` — the single source of truth for XDG app dirs (global.ts:10) + rename notes.
- `design-cli-only.md` — design + reviewer findings for the agent-CLI direction (default command, the "5-minute timeout" investigation, config).
- `design-permissions.md` — THE permission model: 2 modes — default CODING (native tools == shell tier, broad) vs SECURED/precise control (no general shell, allowlisted native tools + specific options, or wrappers; lmprobe = exemplar; IAM-style default-deny). Read before touching tool permissions.
- `metalead-loop.md` — MY core operating method: background N−1 + 1 interactive blocking call = fan-out + wake (no `--detach`). Fleet run command. Read this to run the fleet.
- `lmctl-manual.md` — lmctl operational cheat-sheet for migrating the fleet onto lmctl (chat/jobs/loop, `--detach` removed).
- `lead-brief-lmctl.md` — how the Lead sends via lmctl (chat/seed/auto-resume), fleet.lmctl, and the operator's web console (lmctl.ai, not for me). Docs at lmctl.com.
- `contract-session-metrics.md` — STABLE `session-metrics/v1` JSON contract for `lmplayer session metrics <id> --json` (ORG-METRICS). What lmctl `health`/queries consume; persisted-vs-derived rules, cost formula.
- `integration-lmctl-tokens.md` — lmctl token health contract: read `session` table token columns (or `GET /session/:id .tokens`). Superseded/extended by `contract-session-metrics.md`.
- `remote-poll-channel.md` — CLIENT-ONLY/OUTBOUND-ONLY remote-operator poll channel prototype: `RemoteChannel` (poll/respond) + `RemotePoller`, `run --remote-poll` flags, how it reuses the exact `client.session.prompt` admission path, and the fake-LLM round-trip test. Includes the "copilot Claude must be declared for lmctl" gotcha.
- `provider-ollama.md` — config-free `ollama/<model>` (extended-name `@host`/`OLLAMA_HOST` scheme, seeded built-in provider, on-demand tag synthesis), conditional chat-only for `toolcall:false` models, and the `LMPLAYER_LLM_VERBOSE` file-logging switch. Includes the BUILT-BUT-UNTESTED live-ollama handoff. Commits `108b8bfdf` + `788cc54fc`.
- `provider-ollama-cloud.md` — Ollama CLOUD (`ollama-cloud/<model>`, models.dev catalog provider, `OLLAMA_API_KEY` → ollama.com), how to specify models, and the cloud model list (deepseek/kimi/qwen et al. from `https://ollama.com/api/tags`). Distinct from local `ollama/`; tool-calling support tracked there.
- `evaluation-ollama-cloud.md` — comprehensive evaluation of Ollama Cloud (`ollama_cloud.lmctl` 7 seats): DeepSeek equivalence A/B ($200/mo savings), KimiCode blind reproduction (100% reproduce of agy stderr and lmdbview host-header leaks), reliability battery (1.00 completion rate vs native kimi 0.26), and the critical Capacity Model finding (API exposes zero balance endpoints/headers; client-side token ledgering required).
- `review-2026-08-20-codebase-and-direction.md` — external deep review of dev post job/cron port: pillar re-verification (1+2 hold; 3 broken by ungated notification/cron turns), missing `eaa7ccbf6` claim-liveness port (CHANGELOG wrongly claims it), serve-SIGTERM job-shutdown gap, prioritized next directions.
- `finding-tui-resize.md` — TUI is NON-DEFAULT not disabled (launch `lmplayer tui`/`attach`, PTY-verified). SHIPPED FIX (commit `f1d158f51`): `@opentui@0.4.3` `processResize` (alt-screen) never forces a full repaint (private `forceFullRepaintRequested`/`this.ln`), so a real-terminal resize leaves content cut off; lmplayer's own resize handler (`app.tsx` + `util/renderer.ts forceFullRepaint`) clears `currentRenderBuffer` to a sentinel baseline so every cell repaints regardless of theme. Also: sidebar hidden by default (`routes/session/index.tsx:249` "auto"→"hide"; toggle `<leader> b`). Read for the @opentui render-loop internals + how to test the TUI headlessly.

## How to use / extend this memory

- Start here; open the focused doc for your area.
- After finishing a task, record durable facts (commands, file:line, invariants, gotchas) in the
  relevant doc and update this index. Avoid transient chat state.
- Keep it terse and accurate; verify claims against code before writing.

## Team workflow rules (operator-set)

- ALWAYS commit after a completed, reviewed change. Conventional messages: `type(scope): summary`.
- Process: Lead breaks work into tasks -> Coder implements -> a Reviewer reviews -> fix loop -> Lead commits.
  For complicated design, ask all reviewers; Lead is the technical arbiter and final sanity reviewer.
- Do NOT stage team/harness files: `lmcode.lmctl`, `.mcp.json`, `.opencode/opencode.json`, `.opencode/opencode.jsonc`.

## Build toolchain note (gotcha)

This workspace ships with no `node_modules` and no `bun` on PATH by default. Bootstrap: run `which bun` or
check `$PATH`/`$HOME/.bun/bin` first — the exact install path has been observed to vary by host/VM (seen at
both `/tmp/opencode/.bun/bin/bun` and `~/.bun/bin/bun`); don't hardcode one. Once found, `bun install` at
repo root. Typecheck per package: `bun run typecheck`
(`tsgo --noEmit`). Tests run per package dir (never from repo root — guard `do-not-run-tests-from-root`).
A dev `lmcode` command is installed at `~/.local/bin/lmcode` (runs from source).

## Authoritative source files to cross-check

- `AGENTS.md` (house rules/style), `CONTEXT.md` (V2 session runtime language), `CONTRIBUTING.md`, `package.json`, `turbo.json`.

## Task log (delivered, committed)

- `fix(core)`: XDG app dir `opencode` -> `lmcode` (global.ts:10) so lmcode doesn't collide with a real opencode install.
- `fix(copilot)`: device-flow auth keeps polling on HTTP 400 authorization_pending (was aborting login instantly).
- `feat(cli)`: `models --json` with per-model effort variants.
- `feat(cli)`: `config verify` + readable config errors at verify and launch (shared FormatConfigError).
- `feat(cli)`: `config get/set/unset` (jsonc-safe, schema-validated, global).
- `feat(cli)`: `run --effort` alias + shows `> agent · model · effort` in output.
- `feat(cli)`: `auth list` shows entitled models per authed provider (+ `--json`).
- `chore`: rename CLI binary/command `opencode` -> `lmcode` (bin, build, scriptName, Dockerfile).
- `feat(cli)`: default command sends a prompt; TUI moved to explicit `lmcode tui`; piped stdin runs; bare lmcode -> help.
- `feat(cli)`: `models --test` probes each entitled model (tool-less single turn), reports OK/FAIL, exit non-zero on any fail.
- `feat(opencode)`: `session metrics <id> [--json]` — stable `session-metrics/v1` (tokens+cost_usd+latency+tools+files) read offline from the persisted SQLite store; tokens from the `session` row (fixes lmctl `Tokens: n/a`), cost DERIVED from tokens×model pricing (persisted `session.cost` is 0). Pure `createSessionMetrics` in `packages/opencode/src/cli/cmd/session.ts`; test `test/cli/session-metrics.test.ts`. Contract: `durable-memory/contract-session-metrics.md`.
- `feat(opencode)`: remote-operator poll channel PROTOTYPE (branch `remote-poll-channel`, commit `4ea42dbab`, pushed to lmplayer; Coder=sonnet-5, Reviewer1=gpt-5.5 APPROVE). Client-only/outbound-only in-process poll loop: `RemoteChannel` (poll/respond; HTTP-mailbox + stub) + `RemotePoller` in `packages/opencode/src/remote/`, `run --remote-poll [--poll-token --poll-interval --response-detail]` (hidden, zero change when absent), injects via the exact `client.session.prompt` path, fake-LLM round-trip test `test/remote/poller.test.ts` (5 pass). See `durable-memory/remote-poll-channel.md`.
- `test`: TUI launch-registration + interactive resize regression coverage (commit `ae534e94c`, pushed to lmplayer dev; Coder=sonnet-5, Reviewer1=gpt-5.5 APPROVE-WITH-NITS). Confirms `lmplayer tui`/`attach` reachable (non-default by design) and that the interactive TUI + session message content fully re-render/re-wrap on resize (no cut-off). No functional source change — the reported "TUI disabled" / "resize cut-off" were already-correct/non-reproducible; findings in `durable-memory/finding-tui-resize.md`.
- `feat(opencode)`: config-free Ollama + conditional chat-only + verbose LLM logging (commits `108b8bfdf` Slice 1+3, `788cc54fc` Slice 2, pushed to lmplayer dev; Coder=sonnet-5, Reviewer1=gpt-5.5 adversarial, APPROVE after a 2-blocker fix loop). `--model ollama/<model>` resolves with NO config/key (built-in OpenAI-compatible provider at localhost:11434/v1; `@host` suffix + `OLLAMA_HOST` override; arbitrary tags synthesized on demand); models flagged `toolcall:false` (incl. all ollama) are sent NO tools → plain text (chat-only), capable copilot models unchanged; `LMPLAYER_LLM_VERBOSE=1` writes raw request/stream/tool-parse JSONL to `Global.Path.log/llm-verbose.log`. Additive/isolated: 4 copilot models re-smoked OK. LIVE ollama path is BUILT-BUT-UNTESTED (no ollama on host). Full details + handoff in `durable-memory/provider-ollama.md`.
- `fix(tui)`: force full repaint on resize + hide sidebar by default (commit `f1d158f51`, pushed to lmplayer dev; Coder=sonnet-5, Reviewer1=gpt-5.5 adversarial APPROVE-WITH-NITS). Operator re-raised the cut-off as a REAL physical-terminal bug: `@opentui@0.4.3` `CliRenderer.processResize` (alt-screen, the default for `lmplayer tui`) only diff-renders and never forces a full repaint (unlike resume/capability/split-footer paths), so a resize can leave content truncated with no repaint. Fix: `packages/tui/src/util/renderer.ts` `forceFullRepaint` clears `currentRenderBuffer` to an off-screen sentinel `REPAINT_INVALIDATION_COLOR` (theme-independent — Reviewer1 caught that the default opaque-black clear would skip blank cells under a black theme), wired via `renderer.on("resize", …)` in `app.tsx`. Plus Task C: sidebar hidden by default (`routes/session/index.tsx:249` "auto"→"hide"; toggle `<leader> b`/Ctrl+X b; model/cost stay in the prompt footer + status line). Tests: `packages/tui/test/repaint.test.tsx` (unit) + a wiring test in `test/resize.test.tsx`. Task A needed no change (already launchable; PTY-verified). See `durable-memory/finding-tui-resize.md`.
- `feat(opencode)`: PROFILES — positive tool provisioning + lean system prompt (branch `lean-profiles`, commit `6a2f51d2d`, pushed to lmplayer; Coder=sonnet-5, Reviewer1=gpt-5.5 adversarial → APPROVE-WITH-NITS after a 2-blocker fix loop). Inverts materialize-all-then-deny: an agent may declare `provision: string[]` (positive tool allowlist) so ONLY those tools' model-facing definitions are built/sent regardless of the permission ruleset (`registry.tools()` bypasses `Permission.disabled`; `resolveTools()` is the final gate; infra `invalid`/`_noop` sentinels exempt, never offered). Ship a built-in `lean` agent (`--agent lean`) for weak models (qwen2.5): `prompt`=`session/prompt/lean.txt` (monitor+delegate via lmctl, ~91% smaller than default.txt) + `provision:["bash"]`. Additive — default `build` unchanged. Proof: registry+llm tests assert `body.tools == ["bash"]` under allow-all permission. Legacy path only; V2 `core` `materialize()` is the follow-up seam. Full details in `durable-memory/model-profiles.md`.

- `chore`: upstream refresh, 586 commits from `anomalyco/dev` (merge `bbb19361b`, pushed to lmplayer dev; snapshot regeneration `af0b30248`). Three conflicts in lmplayer's own compaction feature resolved by keeping lmplayer's behavior (upstream's `truncate`/`serialize` prompt rewrite was not adopted); glob/help-text snapshots regenerated separately. See CHANGELOG.md `[Unreleased]`.
- `feat(session)`: ported the session-scoped background-job + cron-scheduler subsystem and a run of concurrency/reliability fixes from a sibling opencode fork (cherry-picked and merged straight to `dev`, no branch — `8b2b7c250`/`9d098f4e8`). Adds persisted background shell jobs (`SessionJobStore`/`SessionJobRuntime`, restart-durable, cross-turn notification delivery), an in-memory cron scheduler (`tool/cron.ts`), SQLite WAL/lock-contention resilience (`sqlite-retry.ts`, periodic WAL checkpoint), ACP event-subscription resubscribe-with-cap, message-ID recency comparison (fixes a ~2.2-year ID-wraparound bug), and bounded compaction-boundary pagination. lmplayer's own pre-existing `BackgroundJob` (in-memory task tracking, `packages/core/src/background-job.ts`) is untouched — different feature, no collision. New integration test (`test/session/prompt.test.ts`, "launches a background job from a real LLM bash tool call...") exercises the full mock-LLM path end to end; full suite green (`packages/core` 1109/1109, `packages/opencode` 3623/3623 modulo pre-existing order-dependent TUI-plugin/attention flake, confirmed 0-fail in isolation).
- `fix(session)`: closed 3 real gaps an external review (Fable, `review-2026-08-20-codebase-and-direction.md`) found in the above port — (1) notification/cron-fired turns now go through the same process-global sequential gate as HTTP turns (`gateSerialize` wrap in `prompt.ts`'s two wake callers; direction pillar 3 was silently broken by the port), (2) cherry-picked the missing `eaa7ccbf6` notification-claim-liveness fix that the original 19-commit port dropped despite the CHANGELOG claiming it landed, (3) `serve`'s SIGTERM path now runs `SessionJobRuntime.shutdown()` before exit, matching the one-shot CLI path (previously orphaned detached job processes). Also: `cron create` now asks permission (was ungated, unlike `job stop`), new `test/server/execution-gate.test.ts` unit-covers the gate mechanism itself. Full findings + prioritized next-direction list in the review doc.
- `fix(provider)`: universal provider timeout defaults + terminal header timeout (commit `cd5f5a932b`; Coder=dsh, Reviewer=kimi APPROVE-WITH-NITS, Lead=agy). Generalized the OpenAI stall bounds across ALL providers at the `resolveSDK` fetch wrapper (header 300s, SSE idle chunk 300s, total 1800s); explicit numeric overrides and `false` opt-outs preserved. Made `HeaderTimeoutError` terminal in `retry.ts` to halt 5x retry cascades. Widened `chunkTimeout` config schema to allow `false` opt-out and regenerated OpenAPI/SDK artifacts. Documented the non-provider turn deadline gap in `backlog/design-universal-provider-timeouts.md`. Tests: 71 pass in header-timeout and retry suites; clean typecheck.
- `fix(server)`: Tier 1 in-process aggregate turn deadline on execution gate (Coder=dsh, Reviewer=kimi APPROVE-WITH-NITS, Lead=agy). Enforced a default 45-minute (2,700,000 ms) wall-clock execution deadline inside `executionGate.serialize`, covering `prompt`, `promptAsync`, `command`, `shell`, `summarize`, `init`, `import`, and background notification/cron turns. Added `TurnTimeoutError` (HTTP 504) schema across `schema`, `core`, `opencode`, and SDK. Precedence: per-call `options.timeoutMs` -> env (`LMPLAYER_TURN_TIMEOUT_MS` / `OPENCODE_TURN_TIMEOUT_MS`) -> `config.turn_timeout_ms` -> default (45m); accepts `false`/`0`/`"off"` to disable. Expiry interrupts the turn fiber, releases permit, runs `onDeadline` to abort prompt runner and finalize assistant message, and cleans up tracked session timeout. Tests: 10/10 `execution-gate.test.ts` unit and `prompt-timeout.test.ts` integration pass; 18 `config.test.ts` pass; clean turbo typecheck.
- `feat(tool)`: durable_memory on-demand read and write tool (Lead=agy). Added first-class agent tool allowing models to read, write, and append session durable memory (`durable-memory/index.md`) continuously during turns. Implemented `action=read` (returns memory or empty notice), `action=write` (replaces memory with provided markdown), and `action=append` (joins with newline separation). Added permission `durable_memory` to default agent permissions and denied in `general` subagent. Registered in `ToolRegistry.builtin`. Tests: 7/7 `durable-memory.test.ts` unit tests pass; 20/20 `registry.test.ts` pass; 11/11 core `agent.test.ts` pass; clean turbo typecheck.
- `feat(cli)`: non-interactive API key login for providers command (Lead=agy). Enabled non-interactive/scripted credentials configuration via `opencode providers login` / `auth login`. Added `--key` / `--api-key` / `-k <key>` flag, piped stdin via `--key -`, and non-TTY piped stdin autodetection with clear fallback error. Integrated with plugin auth (`handlePluginAuth` auto-selecting API auth method when key is provided), and standard provider credentials storage. Guarded provider selection and prompts in non-interactive mode to prevent prompt hangs. Tests: 9/9 `providers-login.test.ts` pass; 10/10 `plugin-auth-picker.test.ts` pass; clean turbo typecheck across all 36 packages.
- `feat(cli)`: deterministic prompt-bypass for agent create (Lead=agy). Added `--prompt`, `--prompt-file`, `--name`, and `--provision` flags to `opencode agent create` to bypass LLM generation and enable fully deterministic, non-interactive agent creation. Derives name from prompt file basename when omitted, writes markdown with grey-matter frontmatter (mode, description, permissions, model, provision), guards against collisions and missing non-interactive fields, and emits generated file path. Tests: 10/10 `agent-create.test.ts` pass; clean turbo typecheck across all 36 packages.
- `feat(cli)`: project scope support for mcp add command (Lead=agy). Added `--scope <project|global>`, `--project` (`-p`), and `--global` (`-g`) options to `opencode mcp add`. Enables non-interactive/scripted agent runs to configure MCP servers directly in project configuration files (`.opencode/opencode.json` or `opencode.json`), with fallback to working directory when not in git repo, mutual exclusion validation, and backward-compatible default to global configuration. Tests: 7/7 `mcp-add.test.ts` pass; clean turbo typecheck.
- `feat(cli)`: session rename, fork, share, and compact commands (Lead=agy). Added CLI subcommands under `session`: `rename <sessionID> <title>`, `fork <sessionID> [--message <id>]`, `share <sessionID> [--unshare]`, `unshare <sessionID>`, and `compact <sessionID> [--model <model>] [--auto]` (aliased to `summarize`). Refactored session mutating commands to communicate consistently via `localSdk()` to honor server workspace routing and HTTP routers. Fixed CLI subprocess test runner harness isolation (`cli-process.ts`) by ensuring fixture `OPENCODE_DB` and `PWD` are set. Tests: 20/20 assertions in `session-commands.test.ts` pass; clean monorepo turbo typecheck.
- `feat(cli)`: project scope support and shadowing warnings for config get/set/unset (Lead=agy). Added `--scope <project|global>`, `--project` (`-p`), and `--global` (`-g`) flags to `opencode config get`, `set`, and `unset`. Implemented `Config.updateProject`, `Config.unsetProject`, and `Config.getProject` with JSONC comment preservation, schema validation prior to write, directory auto-creation (`.opencode/` or root `opencode.json`), and in-memory instance cache invalidation. Added shadowing detection warning on `stderr` when a configured key is overridden by higher-precedence configuration. Enhanced `config verify` to report both global and project candidate provenance. Tests: 9/9 assertions in `test/cli/config.test.ts` and 108/108 in `test/config/config.test.ts` pass; clean monorepo turbo typecheck.
- `feat(cli)`: session jobs inspection and metrics integration (Lead=agy). Added CLI subcommand `lmplayer session jobs <sessionID> [--json] [--output <jobID>] [--job <jobID>] [--status <status>]` for listing and inspecting background jobs and their stdout/stderr logs. Extended `session-metrics/v1` schema to include additive `jobs: { total, active, completed, failed }` breakdown sourced via `sdk.session.jobs` / `SessionJobStore`. Formatted table and detail view for interactive CLI use with graceful fallback for empty or missing jobs. Tests: 31/31 assertions pass across `test/cli/session-jobs.test.ts`, `test/cli/session-metrics.test.ts`, and `test/cli/session-commands.test.ts`; clean monorepo turbo typecheck.
- `feat(agent)`: Profiles Phase 2 — per-model auto-selection and config-defined profiles (Lead=agy). Enabled automatic profile selection for models when `--agent` is omitted: auto-selects built-in `lean` profile (bash-only positive provisioning) for weak model families (`qwen*`), selects user-configured profiles matching candidate models via `isModelMatch` (supporting exact, tag-stripped, and wildcard patterns), while strictly preserving explicit `--agent` override precedence. Implemented `Agent.Service.defaultForModel` and `defaultAgentForModel`; wired into `SessionPrompt.createUserMessage` and `executeCommand` in `packages/opencode/src/session/prompt.ts`, as well as HTTP session summarize and experimental tool endpoints. Tests: 53/53 in `test/agent/agent.test.ts`, 75/75 in `test/session/prompt.test.ts`, 82/82 in registry/llm/session-jobs/session-metrics test suites; clean monorepo turbo typecheck.
- `test(session)`: sequential-queue conformance test and memory doc hygiene (Lead=agy). Added mock-harness integration test in `packages/opencode/test/session/prompt.test.ts` asserting that the process-global execution gate strictly prevents any concurrent LLM turns across prompt (HTTP), background job notification, and cron turns across multiple concurrent sessions. Verified that while an HTTP prompt turn holds the gate permit in-flight, neither background job notifications nor cron schedules can enter LLM streaming. Executed doc hygiene pass across durable memory (closing review-2026-08-20 items 5 and 10). Tests: 76/76 in `test/session/prompt.test.ts`, 9/9 in `test/server/execution-gate.test.ts`; clean monorepo turbo typecheck.
- `test(tool)`: cron and job tool unit test suites and permission contract documentation (Lead=agy). Added comprehensive unit test suites in `packages/opencode/test/tool/cron.test.ts` (7 tests) and `test/tool/job.test.ts` (6 tests) covering tool definitions, list, create/get, stop/delete, output pagination, missing argument validation, and explicit permission asks (`cron` pattern `create` and `job` pattern `stop:<jobID>`). Documented the job and cron permission model and profile interactions in `durable-memory/design-permissions.md` and reconciled status roadmap in `durable-memory/review-2026-08-20-codebase-and-direction.md`. Tests: 13/13 assertions pass; clean monorepo turbo typecheck.
- `refactor(background)`: eliminate import aliasing and document BackgroundJob vs SessionJob disambiguation (Lead=agy). Refactored `packages/opencode/src/background/job.ts` to import `Service` and `make` directly from `@opencode-ai/core/background-job`, eliminating the aliased import (`BackgroundJob as CoreBackgroundJob`) in compliance with AGENTS.md style guide. Added explicit disambiguation section in `AGENTS.md` distinguishing the in-memory task/subagent registry (`BackgroundJob`) from persisted shell subprocesses (`SessionJob`), and updated `review-2026-08-20` section 5. Tests: 11/11 in `test/background/job.test.ts`; clean monorepo turbo typecheck.
- `feat(session)`: durable SQLite cron persistence and session crons CLI command (Lead=agy). Added SQLite table `session_cron` in `@opencode-ai/core` schema with cascade deletion on `session.id`, migration `20260918103549_session_cron`, and wired `SessionCronRuntime` to persist schedules to SQLite across process restarts and recovery. Added CLI subcommand `lmplayer session crons <sessionID> [--json] [--cron <cronID>] [--delete <cronID>]` and integrated additive `crons: { total, recurring, one_shot }` metrics breakdown in `session-metrics/v1`. Updated `contract-session-metrics.md` and marked Review 2026-08-20 item 4 as SHIPPED. Tests: 19/19 migration tests, 9/9 cron-runtime tests, 25/25 session-metrics tests, 4/4 session-crons CLI tests, 2/2 session-commands tests; clean monorepo turbo typecheck across all 30 packages.
- `feat(core)`: positive tool provisioning and lean profile in V2 (Lead=agy). Implemented positive tool provisioning (`provision: string[]`) and registered the built-in `lean` profile in `@opencode-ai/core`. Added optional `provision` to `AgentV2.Info` (`@opencode-ai/schema`) and OpenAPI/SDK definitions (`@opencode-ai/sdk`). Updated `ToolRegistry.materialize()` to accept `MaterializeInput` (`Ruleset | { permissions, provision }`), filtering tool definitions to the provision allowlist while preserving specific tool denies and bypassing wildcard denies. Forwarded agent `provision` from `runner/llm.ts` to `tools.materialize()`. Registered `lean` primary agent (`PROMPT_LEAN`, `provision: ["bash"]`) in `AgentPlugin`. Tests: 19/19 in `test/session-runner-tool-registry.test.ts`, 11/11 in `test/agent.test.ts`; clean monorepo turbo typecheck across all 30 packages.
- `feat(cli)`: mcp and provider enable/disable commands (Lead=agy). Added CLI subcommands `mcp enable <name>` and `mcp disable <name>` to toggle `mcp.<name>.enabled` with jsonc preservation, and `providers enable <provider>` and `providers disable <provider>` (with alias `provider`) to modify `disabled_providers` and `enabled_providers`. Both support `--scope <project|global>`, `--project` (`-p`), and `--global` (`-g`) with mutual exclusion checks. Fixed `Config.updateGlobal` and `Config.unsetGlobal` in `packages/opencode/src/config/config.ts` to ensure parent config directories exist (`fs.ensureDir`) prior to writing. Tests: 8/8 in `test/cli/mcp-enable.test.ts` and `test/cli/providers-toggle.test.ts`; clean monorepo turbo typecheck across all 30 packages.
- `feat(cli)`: mcp remove command and config list command (Lead=agy). Added CLI subcommand `mcp remove <name>` (alias `rm`) to delete MCP servers from project or global JSONC/JSON configurations while preserving formatting and comments, and added `config list` (alias `ls`) supporting `--scope <project|global>`, `--project` (`-p`), and `--global` (`-g`) to output formatted JSON configuration. Tests: 14/14 in `test/cli/config.test.ts` and `test/cli/mcp-remove.test.ts`; clean monorepo turbo typecheck across all 30 packages.
- `feat(cli)`: models test subcommand (Lead=agy). Added first-class CLI subcommand `models test [provider]` (supporting `--json`, `--timeout`, and `--concurrency`) to probe entitled models end-to-end via in-process prompt and report OK/FAIL, complementing existing `models verify <model>` and preserving backward-compatible `models --test`. Tests: 20/20 in `test/cli/models.test.ts` and `test/cli/models-cmd.test.ts`; clean monorepo turbo typecheck across all 30 packages.

- `refactor(cli)`: branding and binary invocation cleanup (Lead=agy). Replaced residual hardcoded "opencode" binary invocations in `pr.ts` (`Process.text`, `Process.spawn`) with dynamic resolution (`which("lmplayer") ?? "opencode"`), updated UI logs to `Starting lmplayer...`, fixed `$0: "lmplayer"` in `runMini`, cleaned up `providers login` positional description to `auth provider URL or ID`, updated MCP client identifier in OAuth debug checks to `lmplayer-debug`, replaced session resume hint in splash footer with `lmplayer --mini -s`, aligned `error.test.ts` expectations with `FormatConfigError`, and refreshed CLI help snapshots. Tests: 6/6 `error.test.ts`, 35/35 `help-snapshots.test.ts`, 28/28 `mcp-add`/`mcp-enable`/`mcp-remove`/`providers-login`/`providers-toggle`; clean monorepo turbo typecheck across all 30 packages.
- `feat(config)`: support top-level default_variant configuration and verification (Lead=agy). Added `default_variant` (with `variant` alias) to `ConfigV1.Info` and `Config.Info` schemas in `@opencode-ai/core`, wired schema migration, incorporated into prompt message resolution (`createUserMessage` checks explicit turn `variant` > agent configured `variant` > `config.default_variant`), added optional `fallbackDefault` to CLI `resolveVariant`, updated `config verify` to output `default_variant: <effort>`, and updated non-interactive assistant start header to display effective variant. Tests: 19/19 core config tests, 6/6 variant shared tests, 78/78 prompt tests (including default_variant resolution), 11/11 CLI config tests; clean monorepo turbo typecheck across all 30 packages.
- `fix(packaging)`: align build, publish, docker, and serve branding to lmplayer binary (Lead=agy). Updated `script/build.ts` to provision `lmcode` and `opencode` aliases alongside compiled `lmplayer` binaries in target `dist/*/bin`, updated `script/publish.ts` to write stubs for `lmplayer`/`lmcode`/`opencode`, registered `lmcode` in package.json `bin`, aligned Arch PKGBUILD and Homebrew formula to install `lmplayer` and symlink `lmcode`/`opencode`, updated Dockerfile to copy `lmplayer` and set `ENTRYPOINT ["lmplayer"]`, and aligned `serve.ts` banner to `lmplayer server listening on...`. Tests: 5/5 `packaging.test.ts`, 12/12 `installation.test.ts`, 2/2 `serve-process.test.ts`; clean monorepo turbo typecheck across all 30 packages.
- `feat(core)`: write-time cost persistence and packaging branding alignment (Lead=agy). Implemented `Model.Cost.calculate` computing write-time token cost from model rates (supporting context size tiers, input, output, reasoning, cache read/write); added `resolveInfo` to `SessionRunnerModel` and wired into `createLLMEventPublisher` and `runner/llm.ts` to publish calculated cost in `SessionEvent.Step.Ended`, projecting real cost into `SessionTable.cost` and assistant messages; updated `postinstall.mjs` to provision `lmcode` alias alongside `lmplayer` and `opencode`; aligned CLI branding and path cleaning in `uninstall.ts`, `upgrade.ts`, `splash.ts`, and `index.ts`. Tests: 7/7 `session-runner-tool-events` tests, 14/14 `session-runner-model` tests, 10/10 `session-projector` tests, 5/5 `packaging` tests, 12/12 `installation` tests, 11/11 CLI `config` tests, 25/25 `session-metrics` tests; clean monorepo turbo typecheck across all 30 packages.
- `fix(packaging)`: provision lmplayer platform packages and dual publish wrappers (Lead=agy). Updated `script/build.ts` to build and package both `lmplayer-<plat>` and `opencode-<plat>` platform packages with all binary aliases; updated `script/publish.ts` to provision and publish both `lmplayer` and `opencode-ai` root wrapper packages with targeted `optionalDependencies`; updated `script/postinstall.mjs` to resolve `lmplayer` platform packages prior to falling back to `opencode` packages; updated TUI `sessionEpilogue` in `presentation.ts` to recommend `lmplayer -s`; added comprehensive packaging tests. Tests: 9/9 `packaging.test.ts`, 12/12 `installation.test.ts`, 3/3 TUI presentation and lifecycle tests, 31/31 core tests; clean monorepo turbo typecheck across all 30 packages.
- `feat(session)`: unified background runtime shutdown for crons and jobs (Lead=agy). Added `shutdown()` method to `SessionCronRuntime` clearing in-memory schedules, resetting `wake`, and disabling subsequent ticks and creates; updated `gracefulShutdown` in `execution-gate.ts` and process exit in `index.ts` to shut down both `SessionJobRuntime` and `SessionCronRuntime` concurrently via `Effect.all`; added unit tests verifying entry clearance and tick suppression. Tests: 10/10 `cron-runtime.test.ts`, 7/7 `tool/cron.test.ts`, 9/9 `execution-gate.test.ts`, 4/4 `session-crons.test.ts`; clean monorepo turbo typecheck across all 30 packages.
- `feat(session)`: persisted write-time cost integration in session metrics (Lead=agy). Updated `createSessionMetrics` to prioritize persisted `session.cost` from SQLite with `source: "persisted"`, falling back to query-time derived pricing calculation (`source: "derived"`) or `"unavailable"`. Updated `formatSessionMetrics` human output to display `Cost: $... (persisted)`. Updated contract docs and reconciled Review 2026-08-20 item 6. Tests: 30/30 `session-metrics.test.ts`, 5/5 `session-report`/`session-health`/`session-crons` tests; clean monorepo turbo typecheck across all 30 packages.
- `feat(cli)`: session todo, diff, export, and import subcommands (Lead=agy). Added CLI subcommands under `session`: `todo <sessionID> [--json]` (formatted checklists with `[x]`, `[>]`, `[-]`, `[ ]` and priorities), `diff <sessionID> [--message <id>] [--stat] [--json]` (file patches and diffstat summaries), `export [sessionID] [--sanitize]`, and `import <file>`. Tests: 7/7 `session-todo-diff.test.ts`, 2/2 `session-commands.test.ts`; clean monorepo turbo typecheck across all 30 packages.
- `feat(cli)`: session ls/list pagination and filters, and config path subcommand (Lead=agy). Added `--limit` (aliases `-n`, `--max-count`), `--roots`, and `--search` (alias `-q`) to `session ls` with bounded message queries avoiding fanout; aligned `session list` to support `--limit`, `--roots`, and `--search`; exported `globalConfigFile` and `projectConfigFile` from `config.ts`; added `config path [--scope project|global] [--project|-p] [--global|-g] [--json]` to inspect resolved configuration file paths and active sources provenance. Tests: 21/21 assertions in `test/cli/session-commands.test.ts`, 5/5 in `test/cli/config.test.ts`; clean monorepo turbo typecheck across all 30 packages.
- `feat(cli)`: agent list filters, show, and delete commands (Lead=agy). Added `agent show <name>` (alias `get`) to inspect agent prompt, model, provision, and permissions; added `agent delete <name>` (alias `rm`) with built-in agent protection and candidate file removal; enhanced `agent list` (alias `ls`) with `--mode <all|primary|subagent>` filtering, model & provision indicators, and `--json` support. Tests: 16/16 assertions in `test/cli/agent-create.test.ts`; clean monorepo turbo typecheck across all 30 packages.
- `feat(cli)`: mcp show command and json output for list and auth list (Lead=agy). Added `mcp show <name>` (alias `get`) to inspect detailed server configuration and status; added `--json` support to `mcp list` (alias `ls`) outputting structured server metadata (type, status, enabled, url/command, cwd, headers, env, timeout, oauth); added `--json` support to `mcp auth list`; pinned `mcp show` in CLI help snapshot tests. Tests: 5/5 assertions in `test/cli/mcp-show-list.test.ts` (37 expect calls), 36/36 help snapshot tests; clean monorepo turbo typecheck across all 30 packages.
- `feat(cli)`: session memory command for inspecting and updating durable memory (Lead=agy). Added `session memory <sessionID>` (alias `brain`) with `--json`, `--write`, `--append`, `--file`, and `--clear` flags, backed by `SessionDurableMemory`; updated help snapshot tests. Tests: 3/3 in `test/cli/session-memory.test.ts` (24 expect calls); clean monorepo turbo typecheck across all 30 packages.
- `feat(cli)`: models show and providers show commands for detailed model and provider inspection (Lead=agy). Added `models show <model>` (alias `get`) to inspect context, input, output token limits, capabilities (reasoning, tool calls, attachments, temperature), reasoning-effort variants, token pricing rates, family, release date, and availability status with `--json` and human formatting; added `providers show <provider>` (alias `get`, available under both `providers` and `auth`) to inspect provider authentication status, credentials path, active environment variables, configuration options, and entitled models list with `--json` and human formatting; updated CLI help snapshot tests. Tests: 30/30 in `test/cli/models.test.ts` and `test/cli/models-providers-show.test.ts` (85 expect calls), 36/36 help snapshot tests; clean monorepo turbo typecheck across all 30 packages.
- `feat(cli)`: json output for stats command (Lead=agy). Added `--json` flag to `stats` command (`packages/opencode/src/cli/cmd/stats.ts`) producing structured JSON metrics (`StatsJson`) with total sessions, messages, cost, detailed token breakdowns (input, output, reasoning, cache read/write, total), daily and per-session averages, date ranges, and tool & model usage breakdowns respecting `--tools` and `--models` limits. Exported `runStats` and `formatStatsJson` for direct Effect execution. Added test suite in `test/cli/stats.test.ts` (6/6 tests pass, 39 assertions) and updated CLI help snapshots. Tests: 42/42 across stats, models, and help snapshot suites; clean typecheck.
- `docs(eval)`: comprehensive evaluation of Ollama Cloud (Lead=agy). Completed paired A/B evaluation across 7 seats (`ollama_cloud.lmctl`). Proved DeepSeek model equivalence (~$200/mo savings on $300 spend), KimiCode 100% blind reproduction of known security findings (`lmratelimit/providers/agy.py:302-306` unredacted child stderr leak and `lmdbview` DNS rebinding / host-header issue), 100% completion rate (35/35 dispatches vs native Kimi 0.26 bar), and identified the Capacity Model finding (Ollama Cloud exposes zero API balance endpoints or headers; client-side token accounting required). Report documented in `durable-memory/evaluation-ollama-cloud.md`.
- `feat(cli)`: provider and model filters for stats command (Lead=agy). Added `--provider <id>` and `--model <id>` options to `stats` command (`packages/opencode/src/cli/cmd/stats.ts`), filtering sessions and metrics to matching provider and model turns, computing filtered message counts, costs, and token breakdowns; extended `StatsJson` with `filters` field; added in-process filtering and unit test suite in `test/cli/stats.test.ts` (8/8 tests pass); updated CLI help snapshots. Tests: 44/44 across stats, models, and help snapshot suites; clean monorepo turbo typecheck.
- `feat(cli)`: displayStats filter summary and message cost/token fallback for stats (Lead=agy). Enhanced `displayStats` to render active filter parameters (provider, model, project) within the overview box and aligned row padding across all sections. Improved `aggregateSessionStats` to gracefully fall back to message-level cost and token sums when session row totals are zero. Added `displayStats` unit test coverage in `test/cli/stats.test.ts`. Tests: 45/45 pass; clean monorepo turbo typecheck.
- `feat(cli)`: cross-project global session listing (-a/--all) for session ls and session list (Lead=agy). Added `-a`/`--all` option to `session ls` and `session list` commands to query sessions globally across all projects and workspaces using `Session.Service.listGlobal`. Updated `SessionLsCommand` to query `Session.Service` directly with bounded concurrency for message counts, avoiding HTTP server latency. Updated CLI help snapshots and added CLI subprocess tests in `test/cli/session-commands.test.ts`. Tests: 23/23 assertions in `session-commands.test.ts` pass; clean monorepo turbo typecheck.
- `feat(cli)`: budget tracking and threshold check for stats command (Lead=agy). Added `--budget <amount>` and `--budget-check` to `stats` command (`packages/opencode/src/cli/cmd/stats.ts`), enabling local spend monitoring against predefined credit limits (crucial for zero-balance-API providers like Ollama Cloud). Computes budget metrics (limit, spend, remaining, usage %, exceeded status), renders a dedicated BUDGET summary box in terminal output, embeds `budget` block in `StatsJson`, validates non-negative inputs, and optionally fails with exit code 2 when `--budget-check` is passed and budget limit is reached or exceeded. Added unit and in-process execution tests in `test/cli/stats.test.ts` (13/13 tests pass, 102 assertions), updated CLI help snapshots, and updated CLI documentation. Tests: 49/49 across stats and help snapshot suites; clean typecheck.
- `feat(cli)`: multi-session deletion, rm alias, force, and token/cost metadata in session ls/list (Lead=agy). Enhanced `session delete` (`packages/opencode/src/cli/cmd/session.ts`) to accept multiple session IDs (`delete <sessionID> [extraSessionIDs..]`), registered `rm` alias, added `--force` (`-f`) flag to ignore missing sessions, and added `--json` structured response (`{ deleted, notFound }`). Enriched `session ls --json` and `session list --format json` to include `cost`, `tokens`, and `created` timestamp metadata for caller accounting. Updated CLI help snapshots and extended test suite in `test/cli/session-commands.test.ts` (41 expect calls). Tests: 41/41 pass; clean monorepo turbo typecheck.
- `feat(cli)`: session tail validation, lines/limit options, and metadata in JSON output (Lead=agy). Updated `session tail` (`packages/opencode/src/cli/cmd/session.ts`) to verify session existence via `sdk.session.get` and exit cleanly with code 1 if not found, registered `--lines` with aliases `[-n, --limit]`, and enriched JSON output (`--json`) with message IDs, timestamps, and assistant model/cost/token metadata. Updated CLI documentation in `durable-memory/cli-commands.md` and added test coverage across not-found and text/JSON tail assertions in `test/cli/session-commands.test.ts` (45 expect calls). Tests: 45/45 pass; clean monorepo turbo typecheck.
- `feat(cli)`: session show command and get alias for detailed session inspection (Lead=agy). Added `session show <sessionID>` (alias `get`) to inspect detailed session metadata (title, directory, parent ID, effective model and variant, agent, creation and update timestamps, message and turn counts, token breakdown, total cost, durable memory status, and public share URL) with human terminal formatting and structured JSON output (`--json`). Updated CLI help snapshot tests and added test suite in `test/cli/session-show.test.ts`. Tests: 18/18 expect calls pass, 36/36 help snapshot tests; clean monorepo turbo typecheck across all 30 packages.
- `feat(cli)`: session status command and status enrichment in session show (Lead=agy). Added `session status [sessionID]` to inspect live runtime state (idle, busy, retry) for an individual session or list all active runs across the instance with human formatting and `--json`; enriched `session show` with live runtime status; updated CLI help snapshot tests. Tests: 26/26 expect calls in `test/cli/session-show.test.ts`, 36/36 help snapshot tests; clean monorepo turbo typecheck across all 30 packages.
- `feat(cli)`: agent clone command and copy/cp aliases for agent customization (Lead=agy). Added `agent clone <source> <target>` (aliases `copy`, `cp`) with `--path`, `--scope`, `--description`, `--model`, `--force`, and `--json` to clone built-in or custom agent configurations into new agents while preserving or overriding model, system prompt, tool provision allowlists, and permissions; guards against overwriting native agents; updated CLI help snapshot tests and added test suite in `test/cli/agent-create.test.ts`. Tests: 18/18 tests pass (73 expect calls), 36/36 help snapshot tests; clean monorepo turbo typecheck across all 30 packages.
- `feat(cli)`: models list command, ls alias, and search/filtering options (Lead=agy). Added `models list [provider]` (alias `ls`) with `--search` (`-q`, `--query`), `--reasoning` (`-r`), `--toolcall` (`--tools`), `--attachment` (`--attachments`), and `--min-context` filters, along with `--json`, `--verbose`, and `--refresh` options; added matching search and capability filter flags to root `models [provider]`; set `global: false` on options to avoid subcommand pollution and alias duplication; updated CLI help snapshots and added unit and subprocess test suites across `test/cli/models.test.ts`, `test/cli/models-cmd.test.ts`, and `test/cli/help/help-snapshots.test.ts`. Tests: 46/46 pass across models suites (124 expect calls), 37/37 help snapshot tests pass; clean monorepo turbo typecheck across all 36 packages.
- `feat(cli)`: db info, check, vacuum commands and path json option (Lead=agy). Added comprehensive SQLite database maintenance and diagnostic suite under `db` command (`packages/opencode/src/cli/cmd/db.ts`), including `db info` (aliases `stats`, `status`, `--json`) with file/WAL/SHM sizes, page stats, SQLite version, journal mode, and per-table row counts; `db check` (aliases `verify`, `integrity`, `--json`) verifying database integrity and foreign keys with exit code 1 on corruption; `db vacuum` (aliases `optimize`, `clean`, `--wal`/`--checkpoint`, `--analyze`, `--json`) running `VACUUM`, `PRAGMA wal_checkpoint(TRUNCATE)`, and `PRAGMA optimize`; and extended `db path` with `--json` output (`path`, `wal_path`, `shm_path`), while preserving interactive query runner `$0 [query]`. Added comprehensive test suite in `test/cli/db.test.ts` (16 tests, 67 assertions) and updated CLI help snapshots. Tests: 17/17 pass across db and help snapshot suites (40 snapshots, 115 expect calls); clean monorepo turbo typecheck across all 36 packages.
- `feat(cli)`: file export output, session import title/json, and fast-fail session guards (Lead=agy). Enhanced `session export` (`packages/opencode/src/cli/cmd/export.ts`) with `--output` (`-o`, `--file`) to write session JSON directly to disk, `--json` summary output, and non-interactive TTY fallback to the latest session; upgraded `session import` (`packages/opencode/src/cli/cmd/import.ts`) with `--title` override, `--json` response format, and fail-fast validation on invalid URLs and empty share payloads; added fast-fail session existence validation to `session report` and `session health` (`packages/opencode/src/cli/cmd/session.ts`); updated CLI help snapshots and created test suite in `test/cli/export-import.test.ts` (4 tests, 25 assertions) alongside not-found assertions in `test/cli/session-commands.test.ts` (49 assertions). Tests: 53 assertions pass across export-import and session-commands suites; 40/40 CLI help snapshots pass; clean monorepo turbo typecheck across all 36 packages.
- `feat(cli)`: file path filter and output file options for session diff, custom title for session fork (Lead=agy). Added `--file` (`--path`) to filter session diffs to matching file paths and `--output` (`-o`) to export diffstat, unified patch, or JSON directly to disk in `session diff` (`packages/opencode/src/cli/cmd/session.ts`); added `--title` (`-t`) to `session fork` allowing custom naming on forked sessions with automatic title update; updated builder definitions and CLI execution test suites across `test/cli/session-todo-diff.test.ts` (9 tests, 40 assertions) and `test/cli/session-commands.test.ts` (2 tests, 52 assertions). Tests: 92 assertions pass across diff and session-commands suites; clean monorepo turbo typecheck across all 36 packages.
- `feat(cli)`: todo filtering & output options, report/metrics output file, and jobs fast-fail validation (Lead=agy). Enhanced `session todo` (`packages/opencode/src/cli/cmd/session.ts`) with `--status` (`-s`) and `--priority` (`-p`) filtering, `--search` (`-q`, `--query`) text matching, and `--output` (`-o`) direct file export; extended `formatSessionTodos` with filter status headers; added `--output` (`-o`) direct file export to `session report` and `session metrics`; added upfront session existence lookup in `session jobs` ensuring consistent error handling across subcommands. Updated builder definitions, formatting unit tests, and CLI execution test suite in `test/cli/session-todo-diff.test.ts` (14 tests, 51 assertions). Tests: 51 assertions pass across session-todo-diff suite; clean monorepo turbo typecheck across all 36 packages.
- `feat(cli)`: session health check/threshold and memory file export options (Lead=agy). Added `--output` (`-o`) direct file export, `--threshold` (`-t`, default 80%), and `--check` (exit code 2 on warning threshold breach) to `session health` (`packages/opencode/src/cli/cmd/session.ts`); added `--output` (`-o`) direct markdown/JSON file export to `session memory` (alias `brain`); updated CLI builder definitions and added unit/in-process test suites across `test/cli/session-memory.test.ts` (3 tests, 30 assertions) and `test/cli/session-todo-diff.test.ts` (15 tests, 57 assertions). Tests: 87 assertions pass across session-memory and session-todo-diff suites; clean monorepo turbo typecheck across all 36 packages.
- `feat(cli)`: config verify json/output options, config get/list file export, and models verify json output (Lead=agy). Added `--output` (`-o`) direct file export and `--json` structured report to `config verify` (`packages/opencode/src/cli/cmd/config.ts`), added `--output` (`-o`) direct file export to `config get` and `config list`, exported `ModelsVerifyCommand` from `packages/opencode/src/cli/cmd/models.ts`, and added `--json` structured validation response and `--output` (`-o`) to `models verify`. Tests: 7/7 in `test/cli/models-cmd.test.ts`, 12/12 in `test/cli/config.test.ts`; clean monorepo turbo typecheck.
- `feat(cli)`: providers list export/search/all, providers show file export, and logout force/json handling (Lead=agy). Added `--output` (`-o`) file export, `--search` (`-q`) query filtering, and `--all` (`-a`) catalog provider listing to `providers list` (alias `auth list`); added `--output` (`-o`) file export to `providers show` (alias `auth show`); enhanced `providers logout` (alias `auth logout`) with `--force` (`-f`) flag, `--json` structured response, `--output` (`-o`) file export, and non-interactive missing provider handling. Tests: 12/12 in `test/cli/models-providers-show.test.ts`, 5/5 in `test/cli/providers-cmd.test.ts`, 4/4 in `test/cli/providers-toggle.test.ts`, 38/38 in `help-snapshots.test.ts`; clean monorepo turbo typecheck.
- `feat(cli)`: mcp list/show/auth file export and filtering, mcp logout force and json handling (Lead=agy). Enhanced `mcp list` (alias `ls`) with `--search` (`-q`, `--query`), `--type` (`-t`), `--enabled` filtering, and direct file export (`--output`); enhanced `mcp show` (alias `get`) with direct file export (`--output`); added query and status filtering (`--search`, `--status`) and file export (`--output`) to `mcp auth list`; upgraded `mcp logout` with `--force` (`-f`) flag, structured `--json` output, `--output` (`-o`) file export, and non-interactive validation. Tests: 6/6 tests (70 assertions) in `test/cli/mcp-show-list.test.ts`, 38/38 help snapshot tests; clean monorepo turbo typecheck.
- `feat(cli)`: agent list/show file export and search/native filtering, agent delete force and output handling (Lead=agy). Enhanced `agent list` (alias `ls`) with `--output` (`-o`) direct file export, `--search` (`-q`, `--query`) substring filtering, and `--native` built-in/custom agent filtering; enhanced `agent show` (alias `get`) with direct file export (`--output`); enhanced `agent delete` (alias `rm`) with `--force` (`-f`) flag to exit cleanly when agent is not found and `--output` (`-o`) file export. Tests: 18/18 tests (88 assertions) in `test/cli/agent-create.test.ts`, 38/38 help snapshot tests; clean monorepo turbo typecheck.
- `feat(cli)`: stats direct file export option and table text rendering (Lead=agy). Added `--output` (`-o`) direct file export to `stats` command (`packages/opencode/src/cli/cmd/stats.ts`) supporting both structured JSON and human-readable formatted table text export; extracted `renderStatsLines` and `renderStatsText` to generate table text without terminal ANSI escape sequences; updated CLI help snapshots and expanded test suite in `packages/opencode/test/cli/stats.test.ts` covering builder options, text table rendering, and in-process file exports across JSON and text modes. Tests: 15/15 tests (138 assertions) pass in `test/cli/stats.test.ts`.
- `feat(cli)`: database info, check, vacuum, and path file export options (Lead=agy). Added `--output` (`-o`) direct file export to `db path`, `db info`, `db check`, and `db vacuum` commands (`packages/opencode/src/cli/cmd/db.ts`) supporting both structured JSON and formatted text report exports; updated CLI help snapshots and expanded test suite in `packages/opencode/test/cli/db.test.ts` covering builder options and in-process file exports across JSON and text modes. Tests: 19/19 tests (94 assertions) pass in `test/cli/db.test.ts`.



## Open / TODO (not done)

- Tier 2 External Watchdog: Tier 1 in-process turn deadline is delivered. Tier 2 (external supervisor watchdog in lmctl/lmauto, ~1d) remains to backstop native kernel lockups (e.g. futex deadlocks that stall the JS event loop).
- claude-* via github-copilot: RESOLVED in the current build — `claude-sonnet-5` and `claude-opus-4.8`
  both return OK via github-copilot (re-smoked 2026-07-08 from source, `run --format json` exit 0), alongside
  gpt-5.5 and gemini-2.5-pro. The old 404 (anthropic /v1/messages shim) note is stale. See models-and-effort.md.
- Portal "external location": currently = committed to this repo. If a separate external location is wanted, TBD.
- Packaging leftovers from the rename: RESOLVED (provisioned `lmplayer-<plat>` platform packages and dual publish wrappers alongside `opencode` legacy wrappers).

